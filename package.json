{
  "name": "ai-contributor-ops-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An original, synthetic contributor-operations portfolio demo.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
